#!/usr/bin/perl

use strict;
use warnings;
use Test::More tests=>1;

BEGIN { 
  push @INC, './lib';
}

use Garden;

my $garden = Garden->new(paths=>['./t/templates']);

my $test = $garden->get('dispatch');
ok (ref $test eq 'Garden::Template');

## TODO: the rest of the test, once multiple dispatch is in place.

